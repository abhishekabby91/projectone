# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/
next.user-config.*

# Environment variables
.env*.local

# Common ignores
node_modules
.next/
.DS_Store
.vercel

# TypeScript incremental build cache
*.tsbuildinfo

# Local environment overrides (.env.example is the documented template)
.env

# Design canvas working files.
# The .dc.html artboards and canvas.json are kept — re-seeding a canvas needs
# them. The seeded output is ~2.5MB of packaged editor code regenerated from
# them on every change, so it stays out of the repo.
.design/accounstone-refresh.html
